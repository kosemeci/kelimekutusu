<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="refresh" content="<%= redirectTime %>;url=<%= redirectUrl %>">
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f2f5;
            color: #333;
            text-align: center;
        }
        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            padding: 20px;
        }
        .error-code {
            font-size: 100px;
            color: #E74C3C;
            margin: 0;
        }
        .message {
            font-size: 24px;
            color: #555;
            margin-bottom: 20px;
        }
        .redirect-message {
            font-size: 16px;
            color: #888;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            color: #fff;
            background-color: #3498db;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }
        .button:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-code"><%= errorCode %></div>
        <div class="message"><%= message %></div>
        <div class="redirect-message"><%= redirectMessage %></div>
        <a href="<%= redirectUrl %>" class="button">Ana Sayfaya Git</a>
    </div>
</body>
</html>
