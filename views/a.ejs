<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Makale Page</title>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="header-content">
        <span class="badge">Rahat Kullanın Rahatınıza Bakın</span>
        <h1>Rahat Makale ile Makalelerini Rahatça Dinle!</h1>
        <p>
          SGK, Muhasebe, Ekonomi ve Vergi gibi alanlarda güncel makaleler anında
          cebinde.
        </p>
        <button class="register-btn" onclick="register()">TIKLA KAYIT OL!</button>
      </div>
      <img src="/images/makale.png" alt="Makale Image" class="hero-image" />
    </div>

    <div class="feature-cards">
      <div class="feature-card">
        <span class="icon">👤</span>
        <h3>Kayıt Ol</h3>
        <p>Kayıt formunu doldurarak sisteme kayıt olabilirsin.</p>
      </div>
      <div class="feature-card">
        <span class="icon">📤</span>
        <h3>Makaleler Gelsin</h3>
        <p>Makaleler Whatsapp üzerinden telefonuna gelsin.</p>
      </div>
      <div class="feature-card">
        <span class="icon">🎧</span>
        <h3>Makaleyi Dinle</h3>
        <p>Açılan pencerede makaleyi ister oku ister dinle.</p>
      </div>
    </div>
    <div id="bubbles"></div>

  </div>

  <script src="script.js"></script>
</body>
</html>

<style>
    body {
  margin: 0;
  padding: 0;
  font-family: 'Arial', sans-serif;
  background: linear-gradient(to right, #4b12cb, #2575fc);
  color: #fff;
}

.container {
  text-align: center;
}

.header {
  position: relative;
  padding: 50px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.header-content {
  max-width: 600px;
  text-align: left;
}

.badge {
  background-color: rgba(255, 255, 255, 0.2);
  padding: 5px 10px;
  border-radius: 20px;
  display: inline-block;
  margin-bottom: 20px;
}

.hero-image {
  max-width: 50%;
  height: auto;
}

.register-btn {
  background-color: #ffd700;
  color: rgba(0, 0, 0, 0.8);
  padding: 10px 20px;
  border-radius: 24px;
  border: none;
  cursor: pointer;
}

.register-btn:hover {
  background-color: #e6c200;
}

.feature-cards {
  display: flex;
  justify-content: center;
  margin: 50px 0;
}

.feature-card {
  background-color: #fff;
  color: #000;
  padding: 20px;
  margin: 10px;
  border-radius: 15px;
  width: 200px;
  text-align: center;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  transition: transform 0.3s;
}

.feature-card:hover {
  transform: scale(1.05);
}

.icon {
  font-size: 48px;
  margin-bottom: 10px;
}
#bubbles {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
  z-index: 0;
}

.bubble {
  position: absolute;
  bottom: -50px;
  background-color: rgba(255, 255, 255, 0.6);
  border-radius: 50%;
  animation: moveUp 6s ease-in-out infinite;
}

@keyframes moveUp {
  0% {
    transform: translateY(0);
    opacity: 0.6;
  }
  50% {
    opacity: 1;
  }
  100% {
    transform: translateY(-100vh);
    opacity: 0.1;
  }
}

</style>

<script>
    function createBubbles() {
  const bubblesContainer = document.getElementById('bubbles');
  const numBubbles = 15;

  for (let i = 0; i < numBubbles; i++) {
    const bubble = document.createElement('div');
    bubble.classList.add('bubble');

    // Rastgele boyut
    const size = Math.random() * 30 + 20 + 'px';
    bubble.style.width = size;
    bubble.style.height = size;

    // Rastgele pozisyon
    bubble.style.left = Math.random() * 100 + 'vw';

    // Rastgele animasyon gecikmesi
    bubble.style.animationDelay = Math.random() * 5 + 's';

    bubblesContainer.appendChild(bubble);
  }
}

function register() {
  window.location.href = 'https://makale.rahatyonetim.com/Register';
}

window.onload = createBubbles;

</script>
